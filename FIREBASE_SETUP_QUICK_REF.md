# 🔑 Firebase Setup - Quick Reference

## Your Project Details

**Package Name (Android)**: `com.mindmate.mindmate_ai`  
**Bundle ID (iOS)**: `com.mindmate.mindmateAi`  
**App Name**: `MindMate AI`

---

## 📋 SHA-1 Fingerprint (for Google Sign-In)

```
8B:DD:7F:DE:2E:92:8D:7F:28:D5:2A:83:1F:7E:98:BE:8A:4B:9E:A0
```

**Where to add this:**
1. Go to Firebase Console → Project Settings
2. Scroll to "Your apps" section
3. Click on your Android app
4. Click "Add fingerprint"
5. Paste the SHA-1 above
6. Click "Save"

---

## ✅ Issues Resolved

### Java Version Fix
- **Issue**: Gradle required Java 11+, but JAVA_HOME pointed to Java 8
- **Solution**: Set JAVA_HOME to Java 21 in PowerShell session
- **For Permanent Fix**: Update JAVA_HOME in Windows Environment Variables

To run Gradle commands, always use:
```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
./gradlew [command]
```

### NDK Version Fix
- Updated `android/app/build.gradle.kts` to use NDK 27.0.12077973
- This resolves Firebase plugin compatibility warnings

### Google Services Plugin
- Commented out for now (will enable after Firebase setup)
- Located in `android/app/build.gradle.kts` line 6

---

## 📝 Next Steps

### 1. Create Firebase Project
Go to: https://console.firebase.google.com

### 2. Add Android App
- Package name: `com.mindmate.mindmate_ai`
- Download `google-services.json`
- Place in: `android/app/google-services.json`

### 3. Add iOS App
- Bundle ID: `com.mindmate.mindmateAi`
- Download `GoogleService-Info.plist`
- Place in: `ios/Runner/GoogleService-Info.plist`

### 4. Enable Authentication
- Email/Password ✅
- Google (use SHA-1 above) ✅
- Apple ✅

### 5. Create Firestore Database
- Start in test mode
- Add security rules from NEXT_STEPS_FIREBASE.md

### 6. Install FlutterFire CLI
```bash
dart pub global activate flutterfire_cli
flutterfire configure
```

---

## 🚨 Important Commands

### Get SHA-1 Again (if needed)
```powershell
cd android
$env:JAVA_HOME = "C:\Program Files\Java\jdk-21"
./gradlew signingReport --no-daemon
```

### Clean Build (if issues)
```bash
flutter clean
flutter pub get
cd android
./gradlew clean
```

### Run App
```bash
flutter run
```

---

## 📚 Files Modified

1. ✅ `android/app/build.gradle.kts` - Commented Google Services plugin, updated NDK
2. ⏳ `android/app/google-services.json` - Needs to be downloaded from Firebase
3. ⏳ `ios/Runner/GoogleService-Info.plist` - Needs to be downloaded from Firebase
4. ⏳ `lib/firebase_options.dart` - Will be generated by FlutterFire CLI

---

**Status**: Ready for Firebase Console setup  
**Next**: Create Firebase project and download config files

---

*Generated: December 17, 2025*
